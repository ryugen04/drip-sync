# このファイルをコピーして keystore.properties を作成してください
# keystore.properties は .gitignore に含まれているため、Git管理外になります

# Release Keystore 設定
storeFile=../release-keystore.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=dripsync
keyPassword=YOUR_KEY_PASSWORD

# Keystore の作成方法:
# keytool -genkey -v -keystore release-keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias dripsync
